import React, {useEffect, useState} from 'react';
import {Formik, Form, useField} from 'formik';
import * as Yup from 'yup';
import {Form as Form2, Button} from 'react-bootstrap';

export default function DoubleSmartPasswordInput({
  onValChange,
  onErrorChange,
  className,
}) {
  return (
    <div></div>
  );
}
